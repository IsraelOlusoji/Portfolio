import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Israel Olusoji I. | Frontend Software Developer Portfolio",
  description: "Frontend Software Developer Israel Olusoji I. — Specializing in React, Next.js, and TypeScript. Explore my portfolio of high-performance, user-focused web applications, technical consulting, and UI/UX solutions. Download my resume, read client testimonials, and get in touch for your next project.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
